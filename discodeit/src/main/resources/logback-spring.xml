<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- 공통 변수 정의 -->
  <property name="LOG_PATH" value="./.logs"/>
  <property name="LOG_PATTERN"
    value="%d{yy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>${LOG_PATTERN}</pattern>
    </encoder>
  </appender>

  <!-- 개발 환경 설정 -->
  <springProfile name="dev">
    <!-- 개발용 파일 출력 (상세한 디버그 정보 포함) -->
    <appender name="DEV_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
      <file>${LOG_PATH}/dev.log</file>
      <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
        <fileNamePattern>${LOG_PATH}/dev.%d{yy-MM-dd}.log</fileNamePattern>
        <!-- 개발 환경은 30일 보관 -->
        <maxHistory>30</maxHistory>
        <totalSizeCap>10GB</totalSizeCap>
      </rollingPolicy>
      <encoder>
        <pattern>${LOG_PATTERN}</pattern>
      </encoder>
    </appender>
    <root level="DEBUG">
      <appender-ref ref="CONSOLE"/>
      <appender-ref ref="FILE"/>
    </root>
  </springProfile>

  <springProfile name="prod">
    <appender name="PROD_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
      <file>${LOG_PATH}/prod.log</file>
      <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
        <fileNamePattern>${LOG_PATH}/prod.%d{yy-MM-dd}.%i.log</fileNamePattern>
        <!-- 파일당 최대 100MB -->
        <maxFileSize>100MB</maxFileSize>
        <!-- 30일간 보관 -->
        <maxHistory>30</maxHistory>
        <!-- 전체 로그 크기 제한 10GB -->
        <totalSizeCap>10GB</totalSizeCap>
      </rollingPolicy>
      <encoder>
        <pattern>${PROD_PATTERN}</pattern>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
      <appender-ref ref="FILE"/>
    </root>
  </springProfile>
</configuration>